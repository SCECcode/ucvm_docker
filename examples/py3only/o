
ln -s /usr/local/python3.8.11/bin/python3.8.11 /usr/bin/python3
python3 -m pip install setuptools wheel numpy netCDF4
amazon-linux-extras install -y epel
yum install –y epel-release
yum-config-manager --enable epel
yum install -y hdf5-devel

python3 -m pip install h5py 

cd /app && \
    git clone https://github.com/kmaterna/InSAR_CGM_readers_writers.git && \
    cd InSAR_CGM_readers_writers && \
    ./setup.py install --user   

CMD [ "/bin/bash" ]
